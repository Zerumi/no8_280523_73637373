public class JDBCRemoveElementFromDBTest { /*
    @Test
    public void test() throws SQLException, IOException {
        try (DBCollectionManager manager = new DBCollectionManager()) {
            //manager.removeElementFromDatabase(7L);
            //manager.removeElementFromDatabase(8L);
            //manager.removeElementFromDatabase(9L);
            System.out.println(manager.removeElementFromDatabase(10L));

            Route route = new Route() {{
                setName("ra9");
                setCoordinates(new Coordinates() {{
                    setX(213);
                    setY(342.34F);
                }});
                setCreationDate(Date.from(Instant.now()));
                setDistance(342);
            }};

            manager.addElementToDataBase(route);
            manager.removeElementFromDatabase(23L);
        }
    }*/
}
